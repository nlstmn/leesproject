export const resetRegionS = () => {
  return { type: "Reset_Regions_Data" }
}

export const resetCountries = () => {
  return { type: "Reset_Countries_Data" }
}

export const resetCities = () => {
  return { type: "Reset_Cities_Data" }
}
